//
//  ContentView.swift
//  njoy
//

import SwiftUI

// The main content view for the application
struct ContentView: View {
    var body: some View {
        // Displays the MainView, which serves as the primary interface for the app
        MainView()
    }
}

// Preview provider for SwiftUI Canvas
#Preview {
    // Enables a preview of ContentView in Xcode's SwiftUI Preview
    ContentView()
}
//
